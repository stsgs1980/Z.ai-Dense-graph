# eact outer v - omprehensive eference uide## vervieweact outer is a modern routing library that provides flexible client-side and server-side routing for eact applications. t offers two primary modes of operation ramework ode and ata ode, with first-class ypecript support through automatic type generation.## nstallation and etup### asic nstallation```bashnpm install react-router```### ypecript onfigurationeact outer generates type-specific files for each route. onfigure your `tsconfig.json````json{  "include" ".react-router/types/**/*"],  "compilerptions" {    "rootirs" ".", "./.react-router/types"]  }}```dd `.react-router/` to `.gitignore````txt.react-router/```### xtending ppoadontextefine your app's context type in a `.ts` or `.d.ts` file```typescriptimport "react-router"declare module "react-router" {  interface ppoadontext {    // add context properties here    apilient lient    userd string  }}```## oute efinition atterns### asic oute onfigurationoutes are defined as objects passed to `createrowserouter````tsximport { createrowserouter } from "react-router"const router  createrowserouter(  {    path "/",    omponent oot  },  {    path "/about",    omponent bout  },])```### ested outeshild routes are rendered through an `utlet/` in the parent component```tsxcreaterowserouter(  {    path "/dashboard",    omponent ashboard,    children      { index true, omponent ashboardome },      { path "settings", omponent ettings },    ],  },])function ashboard() {  return (    div      hashboard/h      utlet / {/* enders child route */}    /div  )}```### ayout outes (o ath)reate layout containers without adding  segments```tsxcreaterowserouter(  {    // no path - just a layout    omponent arketingayout,    children      { index true, omponent ome },      { path "contact", omponent ontact },    ],  },])```### ndex outesefine default routes at a path```tsxcreaterowserouter(  {    path "/",    omponent oot,    children      { index true, omponent ome }, // renders at "/"      { path "about", omponent bout },    ],  },])```### ynamic egmentsse colons to define dynamic parameters```tsx{  path "teams/teamd",  loader async ({ params })  {    return await fetcheam(params.teamd)  },  omponent eam,}```ultiple dynamic segments```tsx{  path "posts/postd/comments/commentd",  // params.postd and params.commentd available}```### ptional egments```tsx{  path "lang/categories" // lang is optional}```### plat outes (atchall)```tsx{  path "files/*",  loader async ({ params })  {    const splat  params"*"] // remaining  after "files/"  },}```## ypecript yping### oute ype mportsn ramework ode, import route-specific types from the generated `+types` directory```tsximport type { oute } from "./+types/product"export async function loader({ params } oute.oaderrgs) {  // params is typed as { id string }  return { planet `world #${params.id}` }}export default function roduct({  loaderata, // typed from loader} oute.omponentrops) {  return h{loaderata.planet}/h}```### enerated ypeseact outer generates these types for each route- `oute.oaderrgs` - oader function arguments- `oute.lientoaderrgs` - lient loader arguments- `oute.ctionrgs` - ction function arguments- `oute.lientctionrgs` - lient action arguments- `oute.omponentrops` - rops for default export- `oute.rroroundaryrops` - rops for rroroundary- `oute.ydrateallbackrops` - rops for ydrateallback### anual oader ata ypingn ata ode, type loader data manually```tsxexport async function loader() {  return { invoices await getnvoices() }}export default function nvoices() {  const data  useoaderatatypeof loader()  // data is typed as { invoices ... }}```### ction ata yping```tsxexport async function action({ request } oute.ctionrgs) {  const formata  await request.formata()  return { message `ello, ${formata.get("name")}` }}export default function orm() {  const actionata  usectionatatypeof action()  // actionata is typed correctly}```## ata oading with oaders### asic oader atternoaders provide data to route components before rendering```tsxcreaterowserouter(  {    path "/posts/postd",    loader async ({ params })  {      const post  await fetchost(params.postd)      return { post }    },    omponent ost,  },])function ost() {  const { post }  useoaderata()  return h{post.title}/h}```### oader rguments```tsxtype oaderunctionrgs  {  params arams //  parameters  request equest // fetch  equest  context ppoadontext // shared context}```### rror andling in oadershrow errors with `data()` function for client-side error boundaries```tsximport { data } from "react-router"export async function loader({ params } oute.oaderrgs) {  const record  await fakeb.getecord(params.id)  if (!record) {    throw data("ecord ot ound", { status  })  }  return record}```### evalidation ontrolontrol when loaders rerun```tsxexport const shouldevalidate houldevalidateunctionrgs  (args)  {  // eturn true to revalidate, false to skip  // efault true when params change or after successful action  return true}```### azy oadingoad components and loaders on-demand```tsx{  path "/app",  lazy async ()  {    const { omponent, loader }  await import("./app")    return { omponent, loader }  },}```## ctions and orm andling### asic ction atternctions handle form submissions and mutations```tsxcreaterowserouter(  {    path "/projects",    action async ({ request })  {      const formata  await request.formata()      const title  formata.get("title")      const project  await createroject({ title })      return project // data available via usectionata    },    omponent rojects,  },])```### orm ubmission ethods#### sing `orm` omponentauses navigation and adds history entry```tsximport { orm } from "react-router"export default function reatevent() {  return (    orm action"/events" method"post"      input type"text" name"title" /      button type"submit"reate/button    /orm  )}```#### sing `useubmit` ookmperative form submission```tsximport { useubmit } from "react-router"export default function imer() {  const submit  useubmit()  const handleimeout  ()  {    submit(      { quizimedut true },      { action "/end-quiz", method "post" }    )  }  return div{/* ... */}/div}```#### sing `useetcher` ookubmit without navigation (no history entry)```tsximport { useetcher } from "react-router"export default function ask() {  const fetcher  useetcher()  return (    fetcher.orm method"post" action"/update-task/"      input type"text" name"title" /      button type"submit"ave/button    /fetcher.orm  )}```### orm alidationeturn errors with non-xx status codes```tsxexport async function action({ request } oute.ctionrgs) {  const formata  await request.formata()  const email  tring(formata.get("email"))  const errors ecordstring, string  {}  if (!email.includes("")) {    errors.email  "nvalid email"  }  if (bject.keys(errors).length  ) {    return data({ errors }, { status  }) // on't revalidate  }  await createser({ email })  return redirect("/dashboard")}export default function ignup() {  const fetcher  useetcher()  const errors  fetcher.data.errors  return (    fetcher.orm method"post"      input name"email" /      {errors.email && span{errors.email}/span}      buttonign p/button    /fetcher.orm  )}```### ccessing ction ata```tsximport { usectionata } from "react-router"export default function roject() {  const actionata  usectionatatypeof action()  return (    div      orm method"post"        {/* form fields */}      /orm      {actionata && puccess {actionata.message}/p}    /div  )}```## avigation atterns### ink omponentor client-side navigation without active styling```tsximport { ink } from "react-router"ink to"/dashboard"ashboard/inkink to{{  pathname "/search",  search "qreact",  hash "#results"}}  earch/ink```### avink omponentor navigation with active/pending states```tsximport { avink } from "react-router"avink to"/messages"  {({ isctive, isending })  (    span classame{isctive  "active"  ""}      essages    /span  )}/avink```### rogrammatic avigation```tsximport { useavigate } from "react-router"export default function ogoututton() {  const navigate  useavigate()  return (    button onlick{()  navigate("/")}      o ome    /button  )}```### avigate ptions```tsxnavigate("/path", {  replace true,              // on't add to history  state { from "/" },       // ass state  relative "route",          // r "path"  preventcrolleset true,   // on't scroll to top  viewransition true,       // nable view transition})navigate(-)  // o backnavigate()   // o forward```### edirect in oaders/ctions```tsximport { redirect } from "react-router"export async function loader({ request } oute.oaderrgs) {  const user  await getser(request)  if (!user) {    return redirect("/login")  }  return { user }}export async function action() {  const project  await createroject()  return redirect(`/projects/${project.id}`)}```##  arameters and earch arams### oute arametersccess dynamic route parameters```tsximport { usearams } from "react-router"export default function ost() {  const params  usearams{ postd string }()  // params.postd is typed as string  return host {params.postd}/h}```### earch arametersandle query strings```tsximport { useearcharams } from "react-router"export default function earch() {  const searcharams, setearcharams]  useearcharams()  const query  searcharams.get("q")  const page  searcharams.get("page") || ""  return (    div      input value{query} /      button onlick{()  setearcharams({ q "react" })}        earch      /button    /div  )}```### etting earch arams```tsx// tringsetearcharams("tab")// bjectsetearcharams({ tab "" })// ultiple valuessetearcharams({ brand "nike", "reebok"] })// allbacksetearcharams((prev)  {  prev.set("tab", "")  return prev})```## rror andling### ramework ode rror oundaries```tsximport { oute } from "./+types/root"import { isouterroresponse } from "react-router"export function rroroundary({ error } oute.rroroundaryrops) {  if (isouterroresponse(error)) {    return (      div        h{error.status} {error.statusext}/h        p{error.data}/p      /div    )  }  if (error instanceof rror) {    return (      div        hrror/h        p{error.message}/p      /div    )  }  return hnknown rror/h}```### ata ode rror oundaries```tsximport { useouterror, isouterroresponse } from "react-router"function ootrroroundary() {  const error  useouterror()  if (isouterroresponse(error)) {    return (      div        h{error.status}/h        p{error.data}/p      /div    )  }  return hrror/h}createrowserouter(  {    path "/",    omponent oot,    rroroundary ootrroroundary,  },])```### hrowing rrorsnintentional errors are caught```tsxexport async function loader() {  return undefined() // rror thrown and caught}```ntentional errors with status codes```tsximport { data } from "react-router"export async function loader({ params } oute.oaderrgs) {  const post  await getost(params.id)  if (!post) {    throw data("ot ound", { status  })  }  return post}```## dvanced ooks### useoaderataet data from the route's loader```tsxexport default function omponent() {  const data  useoaderatatypeof loader()  // fully typed}```### usectionataet data from the last action submission```tsxexport default function orm() {  const actionata  usectionatatypeof action()  return actionata  p{actionata.message}/p  null}```### useetcherndependent data submission without navigation```tsxconst fetcher  useetcher()fetcher.state // "idle" | "loading" | "submitting"fetcher.data // data from action/loaderfetcher.orm // component for formsfetcher.load(path) // load datafetcher.submit(data, options) // submit datafetcher.reset() // reset state```eyed fetchers (access from other components)```tsxconst fetcher  useetcher({ key "my-fetcher" })```### useatcheset all active route matches```tsxconst matches  useatches()// rray of { route, pathname, params, data, handle }matches.forach(match  {  console.log(match.data)    // oader data  console.log(match.handle)  // oute handle metadata})```### useocationet current location```tsxconst location  useocation()// {//   pathname "/posts/",//   search "tab",//   hash "#section",//   state { from "/" }// }```### useavigationrack navigation state```tsxconst navigation  useavigation()navigation.state // "idle" | "loading" | "submitting"navigation.location // new location during navigationnavigation.formata // form data being submitted```## dvanced oute eatures### oute andle ropertyttach metadata to routes for use in ancestor components```tsx// oute definitionexport const handle  {  breadcrumb ()  ink to"/posts"osts/ink,  icon "📝",}// ccess in ancestorfunction oot() {  const matches  useatches()  return (    header      {matches        .filter(m  m.handle.breadcrumb)        .map(m  m.handle.breadcrumb())}    /header  )}```### iddlewareun code before/after navigations```tsxexport async function middleware({ request }, next) {  console.log(`tarting ${request.url}`)  await next()  console.log("omplete")}createrowserouter(  {    path "/",    middleware middleware],    loader rootoader,    omponent oot,  },])```### utlet omponentender child routes```tsximport { utlet } from "react-router"function ayout() {  return (    div      nav{/* navigation */}/nav      utlet / {/* hild routes render here */}    /div  )}```### utletontextass data to child routes via utlet```tsx// arentfunction arent() {  return utlet context{{ user { name "ohn" } }} /}// hildimport { useutletontext } from "react-router"function hild() {  const { user }  useutletontext{ user ser }()  return p{user.name}/p}```## omparison with antack outer### ype afety imilaritiesoth eact outer v and antack outer offer first-class ypecript support| eature | eact outer v | antack outer ||---------|-----------------|-----------------|| oute type generation | ✓ utomatic `.react-router/types/` | ✓ utomatic || aram typing | ✓ uto-inferred from path | ✓ uto-inferred || oader data typing | ✓ ia `oute.oaderrgs` | ✓ ia `loader()` return type || ction typing | ✓ ia `oute.ctionrgs` | ✓ ia `action()` return type || earch params typing | ✓ anual with `useearcharams` | ✓ oute-level definition || edirect typing | ✓ tandard function | ✓ tandard function |### ey ifferences**ype efinition evel**- eact outer ile-based type generation (`.d.ts` files)- antack outer oute-level type registration**earch arams**- eact outer untime `earcharams`- antack outer ompile-time schema validation**ested oute yping**- eact outer nherited from parent context- antack outer xplicit inheritance patterns**rror yping**- eact outer `isouterroresponse()` utility- antack outer uilt-in discriminated unions**earning urve**- eact outer amiliar to those migrating from v- antack outer ore opinionated, explicit type patterns## est ractices### . ype our outes onsistently```tsx// n ramework ode, always use the generated typesimport type { oute } from "./+types/my-route"export async function loader({ params } oute.oaderrgs) {  // params is typed}export default function omponent({ loaderata } oute.omponentrops) {  // loaderata is typed}```### . se orms for avigation-ausing utations```tsx// ood ses orm for main app actionsorm method"post" action"/projects"  input name"title" /  buttonreate/button/orm```### . se etchers for on-avigation utations```tsx// ood ses fetcher for secondary updatesconst fetcher  useetcher()fetcher.orm method"post" action"/task/"  input name"status" /  buttonpdate/button/fetcher.orm```### . andle alidation rrors roperly```tsx// eturn with xx status to prevent revalidationif (validationailed) {  return data({ errors }, { status  })}```### . se `redirect()` in oaders/ctions```tsx// refer redirect in loaders/actionsif (!user) {  return redirect("/login")}// ver useavigate in componentsconst navigate  useavigate() // eserve for rare cases```### . everage `useatches()` for ayout ata```tsx// ccess parent route data in nested componentsconst matches  useatches()const parentata  matchesmatches.length - ].data```## outer reation### ata ode (createrowserouter)```tsximport { createrowserouter, outerrovider } from "react-router"const router  createrowserouter(  { path "/", omponent ome },  { path "/about", omponent bout },])export default function pp() {  return outerrovider router{router} /}```### ramework ode etupse the framework convention with `routes.ts````ts filenameapp/routes.tsimport { route, type outeonfig } from "react-router/dev/routes"export default  route("/", "./routes/index.tsx"),  route("about", "./routes/about.tsx"),  route("posts/id", "./routes/post.tsx"),] satisfies outeonfig```### outer ptions```tsxconst router  createrowserouter(routes, {  basename "/app",                    //  base path  future { v_... },                 // uture flags  hydrationata serverata,          //  hydration  datatrategy customtrategy,       // ustom data loading  getontext createontext,          // ustom context  patchoutesnavigation patchn,  // ynamic routes  unstable_instrumentations logger] // nstrumentation})```## onclusioneact outer v provides a mature, type-safe routing solution with automatic ypecript support. ts familiar  for v users, combined with modern features like middleware, server-side rendering support, and comprehensive type generation, makes it a strong choice for building scalable eact applications.he key advantage over alternatives is the seamless integration with existing eact patterns (hooks, components) and the framework's approach to type safety through automatic code generation rather than manual annotations.