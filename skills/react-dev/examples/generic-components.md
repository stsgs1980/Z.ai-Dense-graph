# eneric omponent atternseneric components provide type safety while maintaining reusability. ypecript infers generic type parameters from prop values — no manual type annotations needed at call site.## eneric able omponentull-featured table with typed columns, custom rendering, sorting.```typescripttype olumn  {  key keyof  header string  render (value keyof ], item )  eact.eactode  sortable boolean}type ablerops  {  data ]  columns olumn]  keyxtractor (item )  string | number  onort (key keyof , direction 'asc' | 'desc')  void  classame string}function able({  data,  columns,  keyxtractor,  onort,  classame,} ablerops) {  const sortey, setortey]  eact.usetatekeyof  | null(null)  const sortir, setortir]  eact.usetate'asc' | 'desc'('asc')  const handleort  (key keyof )  {    const newir  sortey  key && sortir  'asc'  'desc'  'asc'    setortey(key)    setortir(newir)    onort.(key, newir)  }  return (    table classame{classame}      thead        tr          {columns.map((col)  (            th key{tring(col.key)}              {col.sortable  (                button onlick{()  handleort(col.key)}                  {col.header}                  {sortey  col.key && (sortir  'asc'  ' ↑'  ' ↓')}                /button              )  (                col.header              )}            /th          ))}        /tr      /thead      tbody        {data.map((item)  (          tr key{keyxtractor(item)}            {columns.map((col)  (              td key{tring(col.key)}                {col.render                   col.render(itemcol.key], item)                   tring(itemcol.key])}              /td            ))}          /tr        ))}      /tbody    /table  )}```sage```typescripttype ser  {  id number  name string  email string  role 'admin' | 'user'  createdt ate}function serable({ users } { users ser] }) {  const sortedsers, setortedsers]  eact.usetate(users)  const handleort  (key keyof ser, direction 'asc' | 'desc')  {    const sorted  ...users].sort((a, b)  {      if (akey]  bkey]) return direction  'asc'  -      if (akey]  bkey]) return direction  'asc'    -      return    })    setortedsers(sorted)  }  return (    able      data{sortedsers}      columns{        { key 'name', header 'ame', sortable true },        {          key 'email',          header 'mail',          render (email)  a href{`mailto${email}`}{email}/a,        },        {          key 'role',          header 'ole',          render (role)  (            span classame{role  'admin'  'badge-admin'  'badge-user'}              {role}            /span          ),        },        {          key 'createdt',          header 'reated',          render (date)  new ate(date).toocaleatetring(),          sortable true,        },      ]}      keyxtractor{(user)  user.id}      onort{handleort}    /  )}```## eneric elect/ropdownype-safe select with custom option rendering, searching.```typescripttype electrops  {  options ]  value  | null  onhange (value )  void  getabel (option )  string  getalue (option )  string | number  placeholder string  disabled boolean  searchable boolean}function elect({  options,  value,  onhange,  getabel,  getalue,  placeholder  'elect...',  disabled  false,  searchable  false,} electrops) {  const search, setearch]  eact.usetate('')  const ispen, setspen]  eact.usetate(false)  const filtered  searchable     options.filter((opt)        getabel(opt).toowerase().includes(search.toowerase())      )     options  const handleelect  (option )  {    onhange(option)    setspen(false)    setearch('')  }  return (    div classame"select-wrapper"      button        onlick{()  setspen(!ispen)}        disabled{disabled}        classame"select-trigger"        {value  getabel(value)  placeholder}      /button      {ispen && (        div classame"select-dropdown"          {searchable && (            input              type"text"              value{search}              onhange{(e)  setearch(e.target.value)}              placeholder"earch..."              autoocus            /          )}          ul            {filtered.map((option)  (              li                key{getalue(option)}                onlick{()  handleelect(option)}                classame{value  option  'selected'  ''}                {getabel(option)}              /li            ))}          /ul        /div      )}    /div  )}```sage```typescripttype ountry  {  code string  name string  flag string}const countries ountry]  { code '', name 'nited tates', flag '🇺🇸' },  { code '', name 'anada', flag '🇨🇦' },  { code '', name 'exico', flag '🇲🇽' },]function ountryelector() {  const country, setountry]  eact.usetateountry | null(null)  return (    elect      options{countries}      value{country}      onhange{setountry}      getabel{(c)  `${c.flag} ${c.name}`}      getalue{(c)  c.code}      searchable      placeholder"elect country"    /  )}```## eneric ist with ender ropslexible list rendering with type-safe render props.```typescripttype istrops  {  items ]  rendertem (item , index number)  eact.eactode  rendermpty ()  eact.eactode  keyxtractor (item , index number)  string | number  classame string  as 'ul' | 'ol' | 'div'}function ist({  items,  rendertem,  rendermpty,  keyxtractor,  classame,  as omponent  'ul',} istrops) {  if (items.length   && rendermpty) {    return {rendermpty()}/  }  return (    omponent classame{classame}      {items.map((item, index)  {        const key  keyxtractor(item, index)        const element  rendertem(item, index)        return omponent  'div'  (          div key{key}{element}/div        )  (          li key{key}{element}/li        )      })}    /omponent  )}```sage```typescripttype ask  {  id string  title string  completed boolean  priority 'low' | 'medium' | 'high'}function askist({ tasks } { tasks ask] }) {  return (    ist      items{tasks}      keyxtractor{(task)  task.id}      rendertem{(task)  (        div classame{`task priority-${task.priority}`}          input type"checkbox" checked{task.completed} readnly /          span classame{task.completed  'completed'  ''}{task.title}/span        /div      )}      rendermpty{()  (        div classame"empty-state"o tasks yet. dd one to get started!/div      )}      as"div"    /  )}```## onstrained eneric omponentsse constraints to ensure required properties or methods.```typescript// onstraint items must have `id` propertytype asd  { id string | number }type ridrops extends asd  {  items ]  renderard (item )  eact.eactode  columns  |  |}function rid extends asd({  items,  renderard,  columns  ,} ridrops) {  return (    div classame{`grid grid-cols-${columns}`}      {items.map((item)  (        div key{item.id} classame"grid-item"          {renderard(item)}        /div      ))}    /div  )}// sage - type must have `id` propertytype roduct  {  id number  name string  price number  image string}rid  items{products}  renderard{(product)  (    div      img src{product.image} alt{product.name} /      h{product.name}/h      p${product.price}/p    /div  )}  columns{}/```## ultiple eneric arametersomponents with multiple type parameters.```typescripttype airrops,   {  pairs rray, ]  renderey (key )  eact.eactode  renderalue (value )  eact.eactode}function eyalueist, ({ pairs, renderey, renderalue } airrops, ) {  return (    dl      {pairs.map((key, value], index)  (        eact.ragment key{index}          dt{renderey(key)}/dt          dd{renderalue(value)}/dd        /eact.ragment      ))}    /dl  )}// sagetype etricey  'cpu' | 'memory' | 'disk'type etricalue  { current number max number }const metrics rrayetricey, etricalue]  'cpu', { current , max  }],  'memory', { current , max  }],  'disk', { current , max  }],]eyalueist  pairs{metrics}  renderey{(key)  strong{key.topperase()}/strong}  renderalue{(val)  (    span      {val.current}/{val.max}    /span  )}/```## eneric orm ield omponenteusable form field with type-safe value handling.```typescripttype ieldrops  {  name string  label string  value  onhange (value )  void  type 'text' | 'number' | 'email' | 'password'  error string  required boolean}function ormield extends string | number({  name,  label,  value,  onhange,  type,  error,  required  false,} ieldrops) {  const handlehange  (e eact.hangeventnputlement)  {    const newalue      type  'number'  (umber(e.target.value) as )  (e.target.value as )    onhange(newalue)  }  return (    div classame"form-field"      label htmlor{name}        {label}        {required && span classame"required"*/span}      /label      input        id{name}        name{name}        type{type}        value{value}        onhange{handlehange}        required{required}        aria-invalid{!!error}        aria-describedby{error  `${name}-error`  undefined}      /      {error && (        span id{`${name}-error`} classame"error"          {error}        /span      )}    /div  )}// sagefunction serorm() {  const name, setame]  eact.usetate('')  const age, setge]  eact.usetate()  return (    form      ormield        name"name"        label"ame"        value{name}        onhange{setame}        type"text"        required      /      ormield        name"age"        label"ge"        value{age}        onhange{setge}        type"number"      /    /form  )}```## eneric odal/ialogype-safe modal with generic result type.```typescripttype odalrops  {  ispen boolean  onlose (result )  void  title string  children (submit (result )  void)  eact.eactode}function odal({ ispen, onlose, title, children } odalrops) {  if (!ispen) return null  const handleubmit  (result )  {    onlose(result)  }  return (    div classame"modal-overlay"      div classame"modal"        header          h{title}/h          button onlick{()  onlose()}×/button        /header        div classame"modal-body"{children(handleubmit)}/div      /div    /div  )}// sagetype serormata  { name string email string }function pp() {  const showodal, sethowodal]  eact.usetate(false)  const formata, setormata]  eact.usetateserormata({    name '',    email '',  })  const handleodallose  (result serormata)  {    if (result) {      console.log('ser submitted', result)    }    sethowodal(false)  }  return (      button onlick{()  sethowodal(true)}dd ser/button      odalserormata        ispen{showodal}        onlose{handleodallose}        title"dd ew ser"        {(submit)  (          form            onubmit{(e)  {              e.preventefault()              submit(formata)            }}            input              value{formata.name}              onhange{(e)  setormata({ ...formata, name e.target.value })}              placeholder"ame"            /            input              value{formata.email}              onhange{(e)  setormata({ ...formata, email e.target.value })}              placeholder"mail"            /            button type"submit"ubmit/button          /form        )}      /odal    /  )}```