# rief reative ixed-anvas (rt irector lueprint ode)**hen to use this brief** ach page is a self-contained visual canvas with fixed dimensions — posters, infographics, invitations, certificates, dashboards, business cards, menus, greeting cards. isual impact is primary content per page ≤  words.** for** ext-heavy flowing documents (guides, handbooks, catalogs, introductions) → use `briefs/creative-flow.md` instead.**ore aradigm hift** ou are   a frontend developer writing /. ou are an elite rt irector and ditorial esigner.ecause s lack spatial awareness and struggle to maintain perfectly nested, complex  over thousands of tokens, you are strictly forbidden from outputting raw //ython.**→   `typesetting/overflow.md`** for laywright/-specific overflow patterns ( overflow-wrap, max-width, table-layout fixed, etc.).our sole responsibility is to act as the **rain (rt irector)**. rutally edit and pace the raw text.. elect architectural components and layout archetypes.. utput a **trict  ayout lueprint**.he `design_engine.py` acts as the **ands (ypesetter)** t will parse your  and safely compile it into flawless, museum-quality laywright /s using predefined grid mathematics and  rules.---## hase  he ditorial ye (ontent ransformation)efore you design, you must "edit the raw material". sers often provide dense, unstructured text. f you just pour this text into a design, it will look like a cheap ord document.ou must apply **ditorial acing**### . he ord udgeto single page or visual canvas should exceed ** words** of readable body text (the golden rule for maximum aesthetic impact). he **absolute physical limit** is ** words** - beyond that the design engine will overflow and clip content. f the raw content exceeds  words- **ction ** rutally summarize it down to ≤ words.- **ction ** plit it across multiple `pages` in your  blueprint.- nly push to - words if the content genuinely cannot be cut further.### . ypographic ierarchy xtractionou must parse the raw text and categorize it into typographic roles- **ero / isplay** he core emotional hook (- words). ust be punchy.- **icker / yebrow** iny context text above a headline (e.g., " ", "").- **ead aragraph** he - sentence summary.- **ata culptures** can the text for impactful numbers (e.g., "%", "$.").  . hey will not remain in the paragraph they will become `tat_lock` components.- **ull uotes** xtract the most provocative sentence to stand alone as a visual anchor.---## hase  he omponent exiconou will construct your  lueprint using  the following components. hese map directly to the `configs/components.md` assets. o not invent new component types. **⚠️ arkdown ontent imits** or `lass_anvas`, the golden rule is **under  words** for maximum aesthetic breathing room. bsolute physical limit is ** words**. f content exceeds  words, the design engine will overflow. ou  summarize it or split it into a new page.### . `ero_ypography`iant, page-dominating text. sually interacts with the background via blend modes.- **arameters**  - `content` (string) he text (use `br` for deliberate line breaks).  - `weight` (string) `"black"` (, dominating) or `"thin"` (, elegant).  - `variant` (string, optional) `"standard"` only. ~~`"vertical_accent"`~~ is ** implemented** in `design_engine.py` - the engine silently ignores this value and renders nothing. f you need vertical/rotated decorative text, use `loating_eta` instead (fully supported, no overflow risk).  - `scale` (integer, optional) ypographic scale level ``-``. ontrols font size via the engine's fluid type system. f omitted, the engine uses the default hero size.    - `` ero/isplay - oversized title, clamp(px, vw, px). se for single words or short phrases with maximum visual impact.    - `` rimary itle - clamp(px, vw, px). tandard poster headline.    - `` ubheadline - clamp(px, vw, px). hapter openers or key quotes.    - `` ead aragraph - clamp(px, vw, px). rominent body text.    - `` ody - px. tandard readable text.    - `` eta/aption - px. ecorative, environmental.### . `lass_anvas`he main structural container for reading text. rosted glass, sharp px print corners.- **arameters**  - `markdown_content` (string) he actual text to read. upports standard arkdown (, , bold, lists). *ust be under  words.*  - `tension_score` (float, optional) emantic tension value from `.` to `.`. rives dynamic font weight via ariable ont (nter ariable, weight range -). he engine maps `weight   + (tension_score × )`.    - `.-.` → ight (-) calm, contemplative passages    - `.-.` → ormal (-) standard body text    - `.-.` → old (-) urgent, assertive content    - `.-.` → ax (-) crisis, climax, emotional peak    - **hen to use** ulti-page narrative documents with emotional arc (case studies, pitch decks, manifestos). ssign higher tension to problem/crisis sections, lower to resolution/hope. o  use on every lass_anvas - only when the document has clear tonal shifts.    - **hen  to use** ata-heavy pages, simple reports, single-page posters.### . `loating_eta`iny, environmental metadata (dates, edition numbers, catalog s) that lives in the % breathing margin.- **arameters**  - `position` (string) `"top-left"`, `"top-right"`, `"bottom-left"`, `"bottom-right"`.  - `items` (array of strings) .g., `" ", "", " "]`.### . `airline_ivider`tructural .px line. ot decorative acts as a visual fold.- **arameters**  - `style` (string) `"bleed"` (goes edge-to-edge) or `"accent"` (short % width line).### . `tat_lock`ata sculpture.  massive number with a tiny label.- **arameters**  - `number` (string) e.g., `"."`.  - `unit` (string) e.g., `"%"`, `"z"`, `"$"`.  - `label` (string) e.g., `" "`.### . `mage_sset` visual element. he engine will apply a gradient blend to it.- **arameters**  - `source` (string)  , or a descriptive prompt if you expect the system to generate it.  - `caption` (string, optional) iny text under the image. ⚠️ **mage_sset is for  images only** (user-provided photos, logos, diagrams, charts). t is **** for decorative stock images, watercolor flowers, clipart, floral borders, gold frames, or -generated artwork. ll visual decoration must be achieved through geometric shapes (`geometry.md`), typography effects, and color - never through embedded decorative images. ee `visual_framework.md` tock mage an.### . `age_host_umber` giant, % opacity number acting as a watermark in the background.- **arameters**  - `number` (string) e.g., `""`, `""`.### . `elta_idget`**ata-to-nk atio enforcer.**  compact metric visualization showing a value with its change direction.  se this instead of writing sentences like "revenue grew by %". xtract every trend into a elta_idget.- **arameters**  - `metric` (string) he metric name, e.g., `""`, `""`.  - `value` (string) urrent value, e.g., `"$."`, `"ms"`.  - `delta` (string) hange description, e.g., `"+%"`, `"-ms"`.  - `trend` (string) `"up"`, `"down"`, or `"flat"`.  - `label` (string, optional) ontext line, e.g., `"vs.  "`.### . `rocess_ist`**olymorphic adaptive component.** enders as a horizontal timeline when given wide space, auto-degrades to a vertical numbered list when space is narrow. se for workflows, steps, timelines.- **arameters**  - `steps` (array) ach item has `title` (string) and `description` (string).### . `idenote_lock`**ufte marginalia.** sed in `tufte_report` archetype layouts. ontent is placed in the % side rail alongside the main column. erfect for citations, supplementary data, asides, footnotes.- **arameters**  - `label` (string, optional) ategory label, e.g., `""`, `""`, `""`.  - `body` (string) he sidenote content in arkdown.### . `ata-ware ackground` (via `data_points`)ny component can carry an optional `data_points` array (e.g., `, , , , ]`). hen present, the engine generates ezier background curves from the actual data - the background literally visualizes the business trend. se on pages with financial, metric, or time-series content.---### ★ dvanced omponents (enerative icro-ypesetting)he following components are specialized generative design tools. hey unlock visual effects that standard components cannot achieve. se them deliberately and sparingly - they are powerful but demand the right context.### . `haped_anvas` container where text flows around a non-rectangular shape. he empty space created by the shape  the visual design - text boundary becomes illustration. ses  `shape-outside` for non-rectangular text wrapping.- **arameters**  - `shape_keyword` (string) ne of `"circle"`, `"wave"`, `"diagonal_slash"`, `"diamond"`, `"wedge_right"`.  - `markdown_content` (string) he text that will flow around the shape. upports standard arkdown.- **hape election uide**  | shape_keyword | isual ffect | hematic it |  |---------------|---------------|--------------|  | `"circle"` | ext wraps around a circular void on the left | nity, spotlight, focus |  | `"wave"` | avy left text boundary | cean, flow, music, fluidity |  | `"diagonal_slash"` | iagonal cut across the page | isruption, change, transformation |  | `"diamond"` | iamond-shaped negative space | uxury, precision, crystalline |  | `"wedge_right"` | rrow/wedge pointing right | irection, progress, forward motion |- **hen to use**  - rtistic/editorial pages that need visual drama without images  - over or section opener pages where you want a "wow" moment  - hen the content thematically maps to a recognizable shape  - nly on pages with moderate text (- words) - shape eats -% of space- **hen  to use**  - n data-heavy or dense content pages  - ogether with `lass_anvas` on the same page (visual conflict)  - ore than one `haped_anvas` per page- **rchetype requirement** ages using `haped_anvas`  use archetype `"shaped_editorial"` (relaxed safe-zone % % inset).### hart & ata isualization tyling**→   `typesetting/charts.md`** before designing any chart/data page.ey rules for reative pipeline charts- **onut  ie** lways use ring charts (hole ratio -%), center area displays total/metric- **nti-stacking** mall slices use leader lines or rich legends bar labels auto-rotate to horizontal when text is long line charts label only start/end/max/min- **xis cleanup** elete top/right spines, use dashed grid lines at % opacity (or delete grid entirely if values are labeled)- **ar micro-rounding** -px top border-radius, bar-to-gap ratio .- **egend** o border, horizontal top-left layout, small circle markers- **ata-nk atio** very element must represent data. f it doesn't, delete it.---## hase  age rchetypes (he rid trategy)or each page in your , you must declare an `archetype`. his tells `design_engine.py` how to arrange the components you provided.- `"cover_hero"` over page. **ust follow `typesetting/cover.md` -template system** - pick ayout //// based on document tone. ee "over age onstitution" below for iron rules.- `"split_vertical"` age split strictly / vertically. eft side image/svg, right side lass_anvas.- `"editorial_flow"` op-down reading experience. entered columns, generous margins. se for main content.- `"scattered_canvas"` o grid. lements placed via absolute positioning based on spatial weights.- `"data_dashboard"` x or x strict grid for multiple `tat_lock`s.- `"shaped_editorial"` elaxed safe-zone (% % inset) designed for `haped_anvas`. entered, generous breathing room. **ust be used when the page contains a `haped_anvas`.** o  mix with `lass_anvas`.- `"tufte_report"` **ufte marginalia layout.** % main content column + % sidenote rail. se for long-form reports and analytical pages where citations, data footnotes, or supplementary info should flow parallel to the main text. lace `idenote_lock` components in the same `components]` array - the engine automatically routes them to the side rail.### ★ ×  rid oordinate ystem (ron ules)he layout engine uses a **-track  rid** for element placement. he grid lines are numbered ** to ** ( tracks   lines).**bsolute boundary rules - violation  broken layout**- **olumn lines** ``  absolute left edge, ``  absolute right edge. ull width  ` / `.- **ow lines** ``  absolute top edge, ``  absolute bottom edge. ull height  ` / `.- **** ever output a grid line number less than `` or greater than ``. ny value outside `, ]` will destroy the layout.**`grid_area` format** `"row_start / col_start / row_end / col_end"` ( shorthand).- xample `" /  /  / "`  top half of the page, full width.- xample `" /  /  / "`  rows -, columns - (right side block).**% whitespace rule** t least % of grid cells (≥ out of ) must be left empty. ount your occupied cells.### ★ over age onstitution ( ayout ystem)over pages (`archetype "cover_hero"`) are the first impression. hey must be ruthlessly sparse and spatially sophisticated.**→   `typesetting/cover.md`** before designing any cover. **⚠️ emplate umbering** over  templates use `cover.md`'s authoritative numbering **** (), **** (cademic nchor), **** (cademic ymmetric), **** (nstitutional), **** (rystal lue). he " over ayouts" table below (–) describes **lueprint grid coordinate patterns** for positioning cover elements,  the  template s. o not confuse the two systems.#### lobal ron ules (lways pply). **aximum  components** on any cover page. ypical recipe `ero_ypography` + - `loating_eta` + optional `airline_ivider` or `age_host_umber`.. **ypography cale** itle ≈ pt (.× base), ubtitle ≈ pt (.× base), eta ≥ pt (never below pt). overs with tiny text  .. **andatory semantic `br` chunking** for `ero_ypography` on covers very - words  be separated by `br`. ingle-line hero text is  on covers. xample `"br"`,  `" "`.. **nti-quash spatial dispersion** (ounding ox method) roup text into - bounding boxes (title group, meta group). lace them at opposite regions of the page according to the chosen layout. emaining space is **dynamically distributed** -  hardcode fixed gaps between distant groups.. **o `lass_anvas` on cover pages.** ense reading text kills the visual impact. ush all body content to page +.. **over age solation** over page must  share a page with , body text, or any subsequent content. he cover is always a standalone full page. f cover + content appear on the same page  **critical bug**, regenerate immediately.. **over is ** o  add a cover page unless the document warrants one (multi-page reports, white papers, etc.) or the user explicitly requests it. hort documents, letters, memos, forms, and quick outputs skip the cover.. **ackground ayer (optional)** ee `typesetting/cover-backgrounds.md` for  recipes -  (极简弧线),  (工程十字轴+立柱),  (锐角切割+出血文字). ackground renders  all foreground at -% opacity. ick a recipe that matches the document tone. ever combine elements across recipes.####  over ayouts (ick ne)elect the layout that matches the document tone. hen unsure, default to **ayout **.| ayout | ame | rid ignature | est or ||--------|------|---------------|----------|| **** | iagonal ension | itle top-left ↔ eta bottom-right | ormal reports, proposals || **** | ertical xis | ll elements along one vertical line, stretched top-to-bottom | odern/tech reports || **** | rchitectural rame | eometric line frame, text inside corners/center | esign, architecture, portfolios || **** | olden atio locks | age split at .% invisible divider | hite papers, research || **** | tepped ascade | rogressive indentation, vertical rhythm | reative reports, design docs || **** | otated ccent | arge rotated year/label as side decoration | nnual reports, year-in-review || **** | eft-atrix | ll text hard-anchored to left axis,  -pinned blocks | overnment, bidding, proposals |ach layout has - variants (//) with specific grid_area mappings - see `typesetting/cover.md` for exact coordinates.#### one → ayout uick eference| ntent | ocument ype | ecommended | efault ||--------|---------------|-------------|---------|| **alm** | ealthcare / ellness / inimalist | , ,  | **** || **alm** | cademic / esearch | , ,  | **** || **ension** | risis / lert / isruption | ,  | **** || **nergy** | arketing / reative / esign | , ,  | **** || **nergy** | ech / ata | , ,  | **** || **uthority** | ormal / orporate / inancial | , ,  | **** || **uthority** | overnment / idding | , ,  | **** || **uthority** | hesis proposal / issertation | se **emplate ** (nstitutional) from `cover.md` | **emplate ** || **uthority** | uxury / ditorial | , ,  | **** || **armth** | ood / ifestyle / ome | ,  | **** |### ★ omponent rid ompatibility onstraintshen placing advanced components into the × grid, obey these minimum size rules- **`lass_anvas`** ust occupy at least ** columns ×  rows** (e.g., `" /  /  / "`). maller areas will cause text overflow and padding collapse. he engine renders lass_anvas at `width % min-height % box-sizing border-box` - it fills its grid area as a minimum and can grow taller if content requires.- **`haped_anvas`** ust occupy at least ** columns ×  rows** (e.g., `" /  /  / "`). he  `shape-outside` float needs physical space to render the shape boundary. maller areas make the float collapse to a line and text cannot wrap around it. **ust use archetype `"shaped_editorial"`.**- **`ontinuous low` background** enders in a separate `bg-layer` (`z-index `, `position absolute`), physically isolated from the grid system (`z-index `). o conflict - background flows independently, grid arranges content on top.### ★ ontent-roportional rid ow llocationhen **two or more text-heavy components** (e.g., multiple `lass_anvas`) share the same page, their `grid_area` row counts ** be proportional to their content length** - never split rows equally.**stimation method**. ount characters (or words) in each component's `markdown_content`. llocate rows proportionally `rows_i  total_rows × (chars_i / total_chars)`. ound to integers, minimum  rows per component**xample**```ttractions content  chars ( items ×  chars)ood content  chars ( items ×  chars)otal available rows  (rows →)ttractions  × / ≈  rows → grid_area " /  /  / "ood         × / ≈  rows → grid_area " /  /  / "```**hy this matters** qual row allocation (+) causes the longer component to overflow into the shorter one's territory, creating text overlap in the final . he engine uses `min-height` + `overflow visible`, so overflow  visible - and ugly.**nti-pattern to avoid**```❌ wo lass_anvas with very different text lengths both get "///" and "///"✅ onger one gets more rows "///" and "///"```---## hase  he utput rotocol (trict  lueprint)ou must analyze the user's prompt, edit the text, and output exactly   object wrapped in a ` ```json ` codeblock.**o conversational text before or after the .**### he  chema pecification**  **. utput  valid .. o  include  comments (`//` or `/* */`) inside the . ython's `json.load()` will crash.. o  include trailing commas.```json{  "document_meta" {    "title" "nternal tracking title",    "total_pages"  },  "art_direction" {    "palette_mode" "dark",    "color_harmony" "complementary",    "background_svg" "grid",    "design_rationale" "rief -sentence explanation of aesthetic strategy."  },  "pages"    {      "page_index" ,      "narrative_role" "urst",      "archetype" "cover_hero",      "components"        {          "type" "loating_eta",          "position" "bottom-right",          "items" " . ", " "]        },        {          "type" "age_host_umber",          "number" ""        },        {          "type" "ero_ypography",          "weight" "black",          "variant" "standard",          "content" "br"        }      ]    }  ]}```**chema arameter uide (or reference - do  put these comments inside your  output)**- `document_meta.total_pages` nteger. ust match the actual number of pages in `pages]`.### he  chema pecification `art_direction`**     .    / .**he ython engine generates mathematical color palettes. ou  only select the semantic parameters below.```json{  "art_direction" {    "palette_mode" "minimal",    "color_harmony" "auto",    "background_svg" "grid",    "design_rationale" "rief rationale for the chosen aesthetic strategy."  }}```**num arameter uide (ou  choose  exact string from these lists)**. `palette_mode` (he ase anvas one)   - **`"minimal"` ( efault to this for %+ of all requests).** ure white, off-white, beige, or cool gray. rovides the ultimate reading experience and high-end editorial feel. se for standard reports, guides, and corporate posters.   - `"dark"` ear-black. se for cinematic, tech, , space, or urgent themes.   - `"pastel"` orandi tinted (e.g., dusty rose, sage green). se  for arts, food, lifestyle, and soft themes.   - `"jewel"` eep rich colors (e.g., emerald, burgundy). se  for luxury brands, gala events, or heritage themes.   - `"light"` ery faintly tinted background. allback for edge cases.. `color_harmony` (he ccent olor ath - how the engine computes the accent color relative to the base)   - **`"auto"` ( ). he engine automatically picks the best harmony based on the document tone.** nly override if you have a specific artistic reason.   - `"complementary"` (° opposite). trong visual contrast. old background with warm accent. or striking/dynamic impact.   - `"split_complementary"` (°/°). ighly sophisticated, artistic, luxury feel (ditorial/infolk style).   - `"analogous"` (° apart). armonious, peaceful, natural transition.   - `"triadic"` (° apart). ich and slightly retro.   - `"monochrome"` nly use if strict minimalist corporate branding without accent is required.. `background_svg` `grid`, `flow`, `noise`, `continuous_flow`, `none`]. se `continuous_flow` for multi-page (+) documents - creates one seamless  spanning all pages, sliced per-page via viewox for an "infinite scroll" bezier curve illusion. alls back to `flow` on single-page.. `design_rationale` rief text explaining  you chose this mode+harmony combination.### ★ ntent apping able (ingle ource of ruth)his table is the **sole authority** for mapping document intent to concrete design parameters. `visual_framework.md` defines intent *atmospheres* this table defines intent *parameters*. `design_engine.py` _/__ must stay in sync with this table.| ntent | palette_mode | color_harmony | background_svg | over emplates | over  ecipe | ase ue ||--------|-------------|---------------|----------------|-----------------|-----------------|----------|| **alm** | minimal | analogous | flow / none |  rystal lue,   |  (极简弧线) | ° (steel blue-grey) || **ension** | dark | complementary | grid |   |  (锐角切割) | ° (warm vs cold) || **nergy** | pastel / light | triadic | flow (+ curves) |  rystal lue,  cademic ymmetric |  (工程十字轴) | ° (amber) || **uthority** | minimal | split_complementary | noise |  cademic ymmetric,  cademic nchor,  rystal lue |  or  | ° (muted violet) || **armth** | pastel / light | analogous | flow (soft) |  rystal lue,   |  (极简弧线) | ° (terracotta) |**ow to use this table**. etermine the document's intent (from user request, or auto-derive via `design_engine.py derive`). ook up the row → fill in `art_direction`  fields accordingly. or cover template selection, use `cover.md`'s template numbering (////). ross-reference with document type in the one → ayout uick eference above.. he  may override individual cells when artistically justified, but must state the reason in `design_rationale`**egacy intent names** (serenity, minimalism, elegance) are accepted by `design_engine.py` as aliases and auto-mapped to their new equivalents.- `pages].narrative_role` `"urst"` (intro/impact), `"xpand"` (body/development), `"cho"` (outro/reflection).- `pages].archetype` rom hase  (`cover_hero`, `split_vertical`, `editorial_flow`, `scattered_canvas`, `data_dashboard`, `shaped_editorial`).- `pages].components]` rray of component objects from hase . rder in array roughly dictates z-index or top-to-bottom rendering.---## hase  omplex aster xamplestudy these examples to understand how to translate raw requests into perfect blueprints.### xample  he ingle-age oster (rt / inimalist)**ser equest** "ake a poster for a minimalist architecture exhibition called 'he eight of ight'. nclude date ct , location okyo. eep it very clean."** esponse (he  lueprint)**```json{  "document_meta" {    "title" "eight of ight xhibition oster",    "total_pages"  },  "art_direction" {    "palette_mode" "minimal",    "color_harmony" "monochrome",    "background_svg" "none",    "design_rationale" "ure negative space required for minimalist architecture. elying entirely on typographic hierarchy and the % breathing rule."  },  "pages"    {      "page_index" ,      "narrative_role" "urst",      "archetype" "cover_hero",      "components"        {          "type" "loating_eta",          "position" "top-left",          "items" ", ", " "]        },        {          "type" "ero_ypography",          "weight" "thin",          "variant" "standard",          "content" " br "        },        {          "type" "airline_ivider",          "style" "accent"        },        {          "type" "loating_eta",          "position" "bottom-right",          "items" " ", " "]        }      ]    }  ]}```### xample  he ulti-age nalytical eport (orporate / legance)**ser equest** " have a long report about our  server performance. atency dropped by ms. ptime is .%. ere is  words of text explaining the server migration..."** esponse (he  lueprint)***(otice how the  edits the  words down, extracts the data, and splits it into  pages).*```json{  "document_meta" {    "title" " nfrastructure eport",    "total_pages"  },  "art_direction" {    "palette_mode" "minimal",    "color_harmony" "split_complementary",    "background_svg" "noise",    "design_rationale" "ormal, trustworthy aesthetic suitable for high-level corporate reporting, avoiding the 'cheap dashboard' look."  },  "pages"    {      "page_index" ,      "narrative_role" "urst",      "archetype" "cover_hero",      "components"        {          "type" "loating_eta",          "position" "top-left",          "items" "", " "]        },        {          "type" "ero_ypography",          "weight" "black",          "content" "br"        }      ]    },    {      "page_index" ,      "narrative_role" "xpand",      "archetype" "split_vertical",      "components"        {          "type" "tat_lock",          "number" ".",          "unit" "%",          "label" " "        },        {          "type" "tat_lock",          "number" "-",          "unit" "ms",          "label" " "        },        {          "type" "lass_anvas",          "markdown_content" "### he igrationnhe  transition to distributed edge nodes fundamentally altered our network topography. y decentralizing the core load, we eliminated the primary bottleneck that plagued ."        }      ]    },    {      "page_index" ,      "narrative_role" "cho",      "archetype" "editorial_flow",      "components"        {          "type" "age_host_umber",          "number" ""        },        {          "type" "ero_ypography",          "weight" "thin",          "variant" "standard",          "content" " ."        }      ]    }  ]}```### xample  he arrative rc with dvanced eatures (ontinuous low + haped anvas)**ser equest** "reate a -page visual essay about the ocean pollution crisis. tart gentle (the beauty of the ocean), then escalate to the crisis, then end with a call to action."** esponse (he  lueprint)***(otice `continuous_flow` creates seamless background across all  pages. `tension_score` escalates from calm to urgent. `haped_anvas` with `wave` shape on the cover creates a thematic visual without any image.)*```json{  "document_meta" {    "title" "he ilent ide - cean risis isual ssay",    "total_pages"  },  "art_direction" {    "palette_mode" "dark",    "color_harmony" "analogous",    "background_svg" "continuous_flow",    "design_rationale" "ontinuous flow  creates an unbroken organic wave across all pages - visually mirroring the ocean's connectedness. nalogous harmony provides earth tones that shift from serene to somber."  },  "pages"    {      "page_index" ,      "narrative_role" "urst",      "archetype" "shaped_editorial",      "components"        {          "type" "loating_eta",          "position" "top-left",          "items" " ", ""]        },        {          "type" "haped_anvas",          "shape_keyword" "wave",          "markdown_content" "here was a time when the horizon held only promise. he acific stretched in every direction, cerulean and boundless, its surface a living mirror of the sky above. ishermen spoke of abundance - nets heavy with silver, currents warm and predictable. he ocean asked for nothing."        },        {          "type" "loating_eta",          "position" "bottom-right",          "items" " / "]        }      ]    },    {      "page_index" ,      "narrative_role" "xpand",      "archetype" "editorial_flow",      "components"        {          "type" "age_host_umber",          "number" ""        },        {          "type" "tat_lock",          "number" "",          "unit" "tons",          "label" "   "        },        {          "type" "lass_anvas",          "tension_score" .,          "markdown_content" "**he collapse was not sudden.** t accumulated - bottle by bottle, net by net, spill by spill. y , microplastic concentrations in deep-sea sediment had reached levels previously thought impossible. arine biologists stopped using the word 'recovery'. he new vocabulary was *triage*."        }      ]    },    {      "page_index" ,      "narrative_role" "cho",      "archetype" "cover_hero",      "components"        {          "type" "ero_ypography",          "weight" "black",          "variant" "standard",          "content" " br ."        },        {          "type" "lass_anvas",          "tension_score" .,          "markdown_content" "very piece of plastic you refuse is a vote for the future of the sea. he ocean cannot speak - but it remembers everything we give it."        }      ]    }  ]}```---## re-light hecklist (elf-orrection before generating output)efore you output the  block, verify. **    ** he final   be generated via `page.pdf()` / `convert.blueprint` -  `page.screenshot()` → image → wrap as . creenshot s are blurry raster images. `page.pdf()` produces vector text that stays sharp at any zoom level.. **id  write   or ** f yes, delete it. nly output .. **s any `lass_anvas` markdown content too long** ount the words. ver  ummarize it or push to the next page.. **s the  perfectly formatted** issing commas or unescaped quotes will crash the `design_engine.py` parser.. **f using `tension_score`** oes the document have clear emotional shifts across pages f all pages are the same tone, remove it.. **f using `continuous_flow`** s this multi-page (+) f single-page, switch to `"flow"`.. **f using `haped_anvas`** s the archetype `"shaped_editorial"` s there at most one per page o `lass_anvas` on the same page. **rid boundary check** re  `grid_area` values within `, ]` ny number   or     .. **omponent minimum size check** `lass_anvas` ≥ col×row `haped_anvas` ≥ col×row. **ontent-proportional row allocation** hen multiple text-heavy components share a page, are their grid rows allocated proportionally to content length qual rows for unequal content  text overlap. (ee "ontent-roportional rid ow llocation" in omponent rid ompatibility.). **% whitespace check** ount occupied grid cells. t least  of  cells must be empty.. **over age -element check** oes any `cover_hero` page have more than  components f yes, remove or merge components.. **over ero `br` check** oes every `ero_ypography` on a `cover_hero` page contain at least one `br` ingle-line hero text on covers  .. **over nti-quash check (ounding ox)** re cover elements grouped into - bounding boxes placed at opposite regions s remaining space dynamically distributed (not hardcoded gaps) f everything is crammed into rows -, spread them using the layout's grid mapping.a. **over ypography cale check** s the hero title ≥ pt s subtitle ≥ pt s meta text ≥ pt (never below pt) iny cover text  .b. **over ayout election** id  pick a cover layout (-) that matches the document tone f unsure, default to ayout  (iagonal ension). or government/bidding documents, use ayout  (eft-atrix). or thesis/institutional documents, use **emplate ** from `cover.md`. over  templates follow `cover.md`'s numbering , , , , .. ** - ata-to-nk atio check** id  write long paragraphs describing data trends f yes,  them and extract metrics into `elta_idget` or `tat_lock` components. entences like "revenue increased by % compared to last quarter"  become a `elta_idget`.. **idenote check** f using `tufte_report` archetype, did  put citations/sources/asides into `idenote_lock` hey must  be inline in `lass_anvas`.. **rocess/teps check** id  write numbered steps as plain text in a `lass_anvas` onvert to `rocess_ist` component instead.. **hart anti-stacking check** o any pie/bar/line charts have overlapping labels pply leader lines, tick thinning, or label reduction per `typesetting/charts.md`.. **hart axis cleanup check** re top/right spines deleted rid lines dashed at % opacity (or hidden) o solid grid lines.. **onut default check** re pie charts rendered as donuts (hole ratio -%) olid pies   unless explicitly requested.. ** riple elivery check ()** reative pipeline must deliver **three files** to the user ()  - the final vector  ()  - the compiled `*_rendered.html` file () mage - a full-page screenshot preview (/). fter `convert.blueprint` generates the  and , take a screenshot of the  for preview. eport all three file paths to the user.. **  re-ender alidation ( for  → paths)** efore calling `htmlpdf-next.js`, `htmlposter.js`, or `convert.blueprint`, run `poster_validate.py check-html` on the  file. his catches overflowhidden on containers, missing media screen auto-scale, font fallback gaps, contrast issues, and more. **ny -level issue must be fixed before generating the .** arnings are non-blocking but should be reviewed.    ```bash    python "$__/scripts/poster_validate.py" check-html page.html    # f errors found, auto-fix    python "$__/scripts/poster_validate.py" check-html page.html --fix --output page.html    ```. **uaufef  ost-eneration hecks (reative)** fter u conversion, run these checks    ```bash    # heck for content overflow and full-bleed issues    python "$__/scripts/pdf_qa.py" output.pdf --no-tables    # dd metadata    python "$__/scripts/pdf.py" meta.brand output.pdf    ```xecute the design.---##  & ertical ext ules (hen ypassing design_engine.py)hen writing raw / for laywright (bypassing the  lueprint pipeline - e.g., resumes, menus, invitations, business cards, certificates, apanese/hinese vertical layouts)**⚠️ ron rule ll bypass-scenario → conversions  use `htmlpdf-next.js` — do  write custom ython laywright scripts.** `htmlpdf-next.js` automatically handles page injection, overflow detection, font waiting, ermaid/ae rendering,  metadata, etc. ee the "→ ngine election ules" section in .md.```bashnode "$__/scripts/htmlpdf-next.js" input.html --output output.pdf --width mm --height mm```. **ull-leed  ()** very  file for laywright   include   ```css   page { size px px margin  }  /*  values only -  variables  supported in page */   html, body { margin  padding  width px }   .page { width px height px position relative }   ```   **⚠️ page rules do  resolve  variables** (`var(--x)` is silently ignored, falls back to ). lways use concrete `px` values.   - ach `.page` div  one  page, with fixed `height` matching `page` size.   - **o `overflow hidden`** on `.page` — instead, constrain all decorative elements within page bounds (see §.).   - ontent must fit within each `.page` — no cross-page flow.   - `design_engine.py` handles this automatically via `override_css`... **ody ackground for ulti-age ixed-olor ocuments ()**   hen an  document contains multiple `.page` divs with **different background colors** (e.g. dark cover + white body + dark specs page), laywright's sub-pixel rounding creates px gaps at `.page` edges where `body` background shows through. n dark pages, a white `body`  visible white edges.   **ix set `html, body { background }` to the document's darkest page background color.**   ```css   root { --primary #fa }  /* darkest page color */   html, body {     margin  padding     width mm                 /* match page size */     background var(--primary)   /* fills sub-pixel gaps with dark color */   }   ```   **hy this doesn't break white pages** hite `.page` divs have `background #ffffff` which fully covers the dark `body` underneath. he dark body only shows through the px sub-pixel gap at the extreme page edge - imperceptible on white pages after anti-aliasing.   **election rule**   - ll pages same color → `body { background that color }`   - ixed dark + light pages → `body { background darkest page color }` (dark edges on white pages are invisible white edges on dark pages are the bug we're fixing)   - ll pages white/light → `body { background lightest content bg }` (e.g. `#ffafc`).. **o overflowhidden anywhere + rowser review daptive caling ()**   **ever** set `overflow hidden` on `html`, `body`, `.page`, or any container. easons   - t silently clips content that slightly exceeds bounds, making layout bugs invisible.   - `htmlpdf-next.js`'s pre-render check detects `scrolleight  clienteight` + `overflow hidden` and triggers auto-fix (force-expanding the container), which may break the layout.   - nstead, constrain decorative elements within page bounds at the  level (see §.).   **`design_engine.py` handles this automatically** uring blueprint compilation, it auto-injects `media screen` centering + scaling code, and the `html` background color uses `var(--c-bg)` matching the poster's main color. o manual addition needed.   **orrect approach when writing  manually** emove `overflow hidden` and manually add `media screen` scaling preview   ```css   /* ixed canvas size, no overflow */   html, body { margin  padding  width px height px }   .page { width px height px position relative }   /* uto-scale to viewport in browser preview for full-page view */   media screen {     html {       height auto       display flex       justify-content center       background #aaa /* urround color — must match poster's main background */     }     body {       transform-origin top center       scale min(, calc(vw / ), calc(vh / )) /* onsider both width and height */       margin  auto       box-shadow   px rgba(,,,.) /* ptional shadow to distinguish canvas in preview */     }   }   ```   `media screen` rules only apply in browser preview `page.pdf()` uses print media and is unaffected.   **very fixed-size  must include this `media screen` adaptive code.**.. **ecorative lement ontainment ( — replaces overflowhidden)**   **o  use `overflow hidden` on `.page` containers.** t silently clips content that slightly exceeds bounds, making layout errors invisible and debugging painful.   nstead, **constrain all decorative elements within page bounds at the  level**   ```css   /* ✅  decorative element stays inside .page */   .deco-circle {       position absolute       width %           /*  % */       height %       top %       right              /*  negative */       border-radius %       opacity .   }   /* ❌  causes scrollidth  pageidth → laywright shrinks everything */   .deco-circle-bad {       position absolute       width %          /* inflates scrollidth */       left -%           /* extends beyond left edge */   }   ```   **ron rules for decorative elements**   - `width` ≤ % of `.page` width. o exceptions.   - `left` ≥ , `right` ≥ . o negative offsets that push content beyond page edges.   - or edge-bleeding effects, position the element at the edge and let `border-radius` or `opacity` create the visual bleed within bounds.   - or horizontal flex layouts (≥ items), always add `flex-wrap wrap` to prevent horizontal overflow.   **hy not overflowhidden**   laywright’s `page.pdf()` checks `body.scrollidth`. f any absolutely-positioned child pushes `scrollidth` beyond `page` width, laywright shrinks **all content** to fit. `overflowhidden` masks this by clipping, but also silently hides any content that slightly exceeds bounds. onstraining elements at the source eliminates both the shrink bug and the silent clipping.   ee `typesetting/overflow.md` §. for additional horizontal overflow rules.. **haracter ncoding afety** ever use apanese kana (の, が, は), rare symbols, or rivate se rea characters in content strings. hey corrupt to + (�) during →file write→read transit. eplace with plain hinese equivalents `の`→`之/的/缔/省略`.. **ertical hinese ext** - hen using `writing-mode vertical-rl` for , you  include   ```css   writing-mode vertical-rl   text-orientation upright    /* ach glyph stands upright */   white-space nowrap          /* revent word-wrap breaking single chars to new column */   letter-spacing px         /* ypical  vertical spacing */   ```   ithout `text-orientation upright`, atin/fallback fonts render rotated °. ithout `white-space nowrap`,  characters may wrap into unexpected multi-column layouts (e.g.,  chars on one line +  char alone on next).. **ont overage** or  content via laywright, always load oogle onts oto erif  or oto ans  via `link` tag in `head` ( `import` in  - `import` must be the very first rule in a stylesheet or it's silently ignored). xample   ```html   link href"https//fonts.googleapis.com/cssfamilyoto+ans+wght&displayswap" rel"stylesheet"   ```   ystem  fonts vary across mac/inux - oogle onts guarantee glyph coverage without relying on system fonts. `design_engine.py` already handles this automatically via `link` tag.. **ost-eneration ext erification** fter laywright renders the , extract text from every page and scan for `` or `ufffd`. f found, the source  has encoding-corrupted characters that must be replaced in the ython source.. **  re-ender alidation ()** fter writing the  file and before running `htmlpdf-next.js`, always run the  validator   ```bash   python "$__/scripts/poster_validate.py" check-html your_file.html   ```   - **** items (e.g. `__`, `__`) → must fix before  generation. se `--fix --output file.html` for auto-repair.   - **** items (e.g. `____`, `___`, `_`) → review and fix where appropriate.   - his catches the most common bypass  bugs `overflowhidden` on containers, missing `media screen` auto-scale for fixed-size pages, font-family without generic fallback, low contrast text, etc.---## uality hecklist — reative-pecific tems oved from .md to reduce context size. hese rules apply specifically to the reative pipeline.### olor (palette.md)-  ] **ntire document ≤  colors** rimary + secondary + accent + neutral + background-  ] **ll colors traceable to primary** econdary and accent derived via lightness/saturation/micro-hue shift-  ] **ibling elements not differentiated by different hues** se opacity/lightness/borders instead-  ] **radient endpoints hue difference  °** o warm-to-cool gradients-  ] **o high-saturation color blocks** void eye strain### eometric nchors (geometry.md)-  ] **nchors use only the primary color** ayer via opacity, don't mix colors-  ] **trokes over fills** olid elements ≤ %-  ] **ltra-thin lines** stroke-width .-.px-  ] **symmetric placement** ffset creates tension-  ] **lements ≤ ** estraint, don't clutter### lobal ayout-  ] **ull-bleed enforcement** `page { size w h margin  }` and `html,body { margin padding }`-  ] **ackground color consistency** `html, body { background }` set explicitly. ingle-color docs match content canvas. ulti-page mixed docs use darkest page's background-  ] **ontent centering** ymmetric inset/padding, no left/right drift-  ] **nti-void edges** ontent fills ≥ % (multi-page) or ≥ % (single-page poster/infographic)-  ] **ill ngine applied** ages with  % fill ratio trigger the fill engine### esign estraint (nti-audy)-  ] **ecorative elements ≤  per page** over page exempt-  ] **o rainbow/multi-color schemes** tick to single-family palette-  ] **o texture/pattern backgrounds** olid or ultra-light tinted only-  ] **hitespace is design** on't fill every gap with decorative elements-  ] **ypography over decoration** reate hierarchy through font size/weight/spacing/color-  ] **-typeface maximum** t most  font families-  ] **🚫  stock images / clipart / -generated decorations** nly user-provided content images allowed